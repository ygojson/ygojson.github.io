---
title: Card Model
description: Card Model
banner:
    content: '🚧 The YGOJSON reference is under construction 🚧'
---

import SchemaComponent from '../../../components/SchemaComponent.astro';

export const cardSchema = {
    title: "Card",
    $id: "card.schema.json",
    description : "Describes the information for a card.\n\nThis model contains the properties of the card that will not change from print to print.",
    properties : {
        id : {
            type : "string",
            format : "uuid",
            description : "UID (v5) for the Card (atomic) generated by YGOJSON."
        },
        name : {
            type : "string",
            description : "Name of the card"
        }
    }
}

<main>
<SchemaComponent schema={cardSchema} />
</main>

